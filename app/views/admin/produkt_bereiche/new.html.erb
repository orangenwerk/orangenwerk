<h1>Produktbereich erstellen</h1>

<%= form_for @produkt_bereich, :html => { :multipart => true }, :url => { :controller => 'admin/produkt_bereiche'} do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>
<% end %>

<%= link_to 'Back', admin_produkt_bereiche_path %>
