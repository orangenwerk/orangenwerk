<h2>AGB Neu</h2>
<%= form_for @agb, :html => { :multipart => true }, :url => { :controller => 'admin/agbs'} do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>
<% end %>

<%= link_to 'Zurück', admin_agbs_path %>
