<%= render 'app_shared/error_msg', :item => f.object %>
<table class="field-list">
<tbody>
	<tr>
		<th class="name-label">
			<%= f.label :geschlecht %>
		</th>
		<td class="name-view">
			<%= f.select :geschlecht, geschlechter.map { |c| [c, c] }, :include_blank => true %>
			<div class="input-help">Einfach halten ! = klein, '_' statt ' ' ! .. Bsp mesh_outdoor</div>
		</td>
	</tr>
	<tr>
		<th class="name-label">
			<%= f.label :name %>
		</th>
		<td class="name-view">
			<%= f.text_field :name %>
			<div class="input-help">Einfach halten ! = klein, '_' statt ' ' ! .. Bsp mesh_outdoor</div>
		</td>
	</tr>
	<tr>
		<th class="name-label">
			<%= f.label :email %>
		</th>
		<td class="name-view">
			<%= f.text_field :email %>
			<div class="input-help">Einfach halten ! = klein, '_' statt ' ' ! .. Bsp mesh_outdoor</div>
		</td>
	</tr>
</tbody></table>
<div class="actions"><%= f.submit %></div>

