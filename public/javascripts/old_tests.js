
/*
$(".kunde_icon_klein").find('img').css('margin-top', function(){
		var box = $(this).parent.outerHeight();
		var pic = $(this).outerHeight();
		var marg = Math.round((box-pic)/2)+'px1';
		return marg
		});

$("img.kunde_icon_kmed").each(function(x) {
        var box = $(this).parent.outerHeight();
		var pic = $(this).outerHeight();
		var marg = Math.round((box-pic)/2);
		$(this).css('margin-top',marg+'px')
  });



$('img.kunde_icon_klein').bind(function(){
	var xcbx = $(this).find("img").outerHeight();
		var xcvx = $(this).outerHeight();
		var marg = Math.round((xcvx-xcbx)/2);
		alert(xcvx+' '+xcbx+' . '+marg);
		$(this).find("img").css('margin-top',marg+'px')
});

$("kunde_icon_klein").each(function (i) {
        if (this.parent.style.height > this.style.height) {
          this.style.margin = "10px 0";
        } else {
          this.style.color = "";
        }
      });
*/




	
//	$(".colorBoxa").colorbox({rel:'group-5'});
	
//	$.colorbox({href:"/hallo"});
	
//	$("#tabs").tabs();


/*

	$(".kunde_icon_klein > .resize_picas").each(function (index, domEli) {
		var xxx = $(this).parent().innerHeight();
		var box = $(domEli).find("img.resize_pic").clientWidth;
		var pic = $(domEli).outerHeight();
		var pix = $(domEli).innerHeight();
		var marg = Math.round((box-pic)/2);
		// var marg = Math.round((xcvx-xcbx)/2);
		// alert('box: '+box+'  xxx:'+xxx+'  pic:'+pic+'  pix:'+pix+' ... = '+marg);

      });

$(".kunde_icon_klein").click(function(){
		var xcbx = $(this).find("img").outerHeight();
		var xcvx = $(this).outerHeight();
		var marg = Math.round((xcvx-xcbx)/2);
		alert(xcvx+' '+xcbx+' . '+marg);
		$(this).find("img").css('margin-top',marg+'px')
	});
	
	
		var marg = Math.round((box-pic)/2);
		alert(box+' - '+pic+'/'+pix+' / 2 = '+marg);
		$(domEli).find("img").css('margin-top',marg+'px')
      });


	$(".kunde_icon_klein").click(function(){
		var xcbx = $(this).find("img").outerHeight();
		var xcvx = $(this).outerHeight();
		var marg = Math.round((xcvx-xcbx)/2);
		alert(xcvx+' '+xcbx+' . '+marg);
		$(this).find("img").css('margin-top',marg+'px')
	});
	
	$("img").click( function() {
		var box = $(this).parent.outerHeight();
		var pic = $(this).outerHeight();
		var marg = Math.round((box-pic)/2);
		$(this).css('margin-top',marg+'px')
		alert(marg);
});
	$('referenz_icon_med_pic').hover(function(){
		$(this).addClass('rounded_box');
	});
	
	
	$(".kunde_icon_klein").each(function (index, domEle) {
		var box = $(domEle).outerHeight();
		var pic = $(domEle).find(".resize_pic").outerHeight();
		var marg = Math.round((box-pic)/2);
		$(domEle).find("img").css('margin-top',marg+'px');
        alert(box+' '+pic+' . '+marg);
      });

	$(".resize_pic").each(function (index, domEli) {
		var boxi = $(this).parent().outerHeight();
		var pici = $(this).outerHeight();
		var margi = Math.round((boxi-pici)/2);
		$(this).css('margin-top',margi+'px');
        alert(boxi+' '+pici+' . '+margi);
      });

*/